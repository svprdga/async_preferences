name: async_preferences
description: A flutter plugin to use local preferences asynchronously.
version: 0.7.0
homepage: https://davidserrano.io

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: 2.1.2

flutter:
  plugin:
    platforms:
      android:
        package: com.svprdga.async_preferences
        pluginClass: AsyncPreferencesPlugin
      ios:
        pluginClass: AsyncPreferencesPlugin